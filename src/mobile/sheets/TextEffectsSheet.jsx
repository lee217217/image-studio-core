import TextEffectsPanel from '../../components/TextEffectsPanel.jsx';

/** Thin mobile-sheet wrapper around TextEffectsPanel. */
export default function TextEffectsSheet({ onClose }) {
  return <TextEffectsPanel onClose={onClose} />;
}
